<% form_for(@site) do |f| %>
  <%= render :partial => 'f', :object => f, :locals => {:legend => "Editing Site"} %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @site %> |
<%= link_to 'Back', sites_path %>