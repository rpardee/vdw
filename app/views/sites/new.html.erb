<% form_for(@site) do |f| %>
<fieldset>
  <legend>New Site</legend>
  <%= render :partial => 'f', :object => f %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', sites_path %>