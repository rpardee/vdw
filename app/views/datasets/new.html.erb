<% form_for(@dataset) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f, :legend => "New Dataset"} %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', datasets_path %>