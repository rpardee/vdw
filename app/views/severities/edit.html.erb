<% form_for(@severity) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'f', :locals => {:f => f, :legend => "Editing Severity"}  %>
  <%= f.submit 'Update' %>
<% end %>

<%= link_to 'Show', @severity %> |
<%= link_to 'Back', severities_path %>