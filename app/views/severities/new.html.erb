<h1>New severity</h1>

<% form_for(@severity) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'f', :locals => {:f => f, :legend => "New Severity"}  %>
  <%= f.submit 'Create' %>
<% end %>

<%= link_to 'Back', severities_path %>